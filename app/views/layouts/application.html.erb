<!DOCTYPE html>
<html>
<head>
  <title>AccessWrapper</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<!-- NAVIGATION MENU -->
  <div class="navbar-nav navbar-inverse navbar-fixed-top">
      <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="index.html">
           <%= image_tag "logo30.png" %>Access Payment Portal
        </a>
      </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <li><a href="/admin"><i class="icon-home icon-white"></i> Home</a></li>
            <% if admin_signed_in? %>
              <li><a href="/admin/homeowners"><i class="icon-folder-open icon-white"></i> Homeowners</a></li>
            <% end %>
            <p class="navbar-text pull-right">
            <% if admin_signed_in? %>
              Logged in as <strong><%= current_admin.email %></strong>.
              <li><%= link_to 'Edit profile', edit_admin_registration_path, :class => 'navbar-link' %></li>
              <li><%= link_to "Logout", destroy_admin_session_path, method: :delete, :class => 'navbar-link'  %></li>
            <% else %>
              <li><%= link_to "Sign up", new_admin_registration_path, :class => 'navbar-link'  %></li>
              <li><%= link_to "Login", new_admin_session_path, :class => 'navbar-link'  %></li>
            <% end %>
            <% if notice %>
              <li><p class="alert alert-notice" style="color:#999"><%= notice %></p></li>
            <% end %>
            <% if alert %>
              <li><p class="alert alert-error" style="color:#999"><%= alert %></p></li>
            <% end %>
            </p>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
  </div>

<%= yield %>

</body>
</html>
